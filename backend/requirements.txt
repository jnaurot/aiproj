# Legacy pip entrypoint.
# Source-of-truth for dependency definitions is backend/pyproject.toml (uv).
# Default backend install profile:
# - core runtime
# - pandas compatibility layer
# - test tooling
-r requirements/core.txt
-r requirements/compat.txt
-r requirements/testing.txt

# Optional profiles (install explicitly as needed):
# -r requirements/llm.txt
# -r requirements/dl.txt
# -r requirements/finetune.txt
# -r requirements/observability.txt
